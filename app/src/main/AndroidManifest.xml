<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CityEventsAndr"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".view.RegisterArtistView"
            android:exported="false" />
        <activity
            android:name=".view.ArtistListView"
            android:exported="false" />
        <activity
            android:name=".view.LocationDetailView"
            android:exported="false" />
        <activity
            android:name=".view.RegisterLocationView"
            android:exported="false" />
        <activity
            android:name=".view.LocationListView"
            android:exported="false" />
        <activity
            android:name=".view.RegisterEventView"
            android:exported="false" />
        <activity
            android:name=".view.EventListView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>